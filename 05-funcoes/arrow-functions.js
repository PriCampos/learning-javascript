//forma simplificada de declarar a função
let soma = (num1, num2) => {
    return num1 + num2
}

console.log(soma(1, 2))

let dobrar = valor => valor * 2 //eliminar as chaves quando houver somente uma linha de execução.

console.log(dobrar(3))

let mensagem = () => console.log('arrow functions')


