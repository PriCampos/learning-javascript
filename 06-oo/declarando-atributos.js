//Para definir os atributos de uma classe é preciso utilizar um método construtor.
//os atributos são caractéristicas de um objeto
//em JS tudo é público
    
class carro {
    constructor(modelo, ano) {
        this.modelo = modelo,
        this.ano = ano
    } 
}

class Pessoa {
    constructor(nome, pronome) {
        this.nome = nome,
        this.pronome = pronome
    }
}